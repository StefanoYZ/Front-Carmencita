[build]
  command = "npm ci --include=optional && npm install --no-save @rollup/rollup-linux-x64-gnu@4.60.2 && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
